def downloadFile(path):
    # Deve fazer o download do arquivo (cujo caminho está especificado como argumento da função), que está registrado no servidor, para o cliente.
    # Faça com que o arquivo seja baixado na pasta root
    return False

def deleteFile(path):
    # Deve deletar o arquivo (cujo caminho está especificado como argumento da função) do diretório do cliente no servidor.
    return False

def uploadFile(path):
    # Deve fazer upload do arquivo (cujo caminho está especificado como argumento da função), que está registrado no cliente, para o servidor.
    return False