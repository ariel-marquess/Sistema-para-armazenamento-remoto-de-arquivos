def record(obj):
    # Este método deve registrar no servidor as informações passadas pelo dicionário 'obj'.
    # O dicionário segue as seguintes chaves: 'username' e 'password'.
    # O 'username' pode ter somente letras e números, então poderia ser usado no servidor como o nome da pasta que armazenará os dados de usuário.
    return False
